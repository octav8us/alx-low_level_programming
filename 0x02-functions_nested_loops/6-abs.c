#include "main.h"
/**
 * _abs - convert int to it's absloute value
 * @n: the number to be computed
 * Return: absloute value of @n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);

}
