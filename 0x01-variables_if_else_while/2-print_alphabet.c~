#include<stdio.h>

/**
 *
 *  main - prints the alphapet
 *
 *  Return : always 0
 *
 */
int main(void)
{
	char i;

	i = 'a';

	for (i; i <=  'z'; i++)
	{
		putchar (i);
	}

	putchar ('\n');
	return (0);
}
